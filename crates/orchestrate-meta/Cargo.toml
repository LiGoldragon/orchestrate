[package]
name = "orchestrate-meta"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Datom client for the privileged Orchestrate Signal."
build = "build.rs"

[[bin]]
name = "orchestrate-meta"
path = "src/main.rs"

[dependencies]
datom-codec.workspace = true
meta-signal-orchestrate = { workspace = true, features = ["datom"] }
protos.workspace = true
signal.workspace = true
thiserror.workspace = true

[build-dependencies]
ethos-zero.workspace = true

[dev-dependencies]
# The meta contract embeds the ordinary contract's configuration types;
# the boundary test names them.
signal-orchestrate = { workspace = true, features = ["datom"] }
tempfile = "3"

[lints]
workspace = true
